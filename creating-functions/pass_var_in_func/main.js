function intro(name) {
  alert("hello this side " + " " + name + " " + "nice to meet u all");
}
// intro('mahesh');
// use of functions properly to find three times of a number
function three_times(x) {
  return 3 * x;
}
let result = three_times(5);
alert('the result is ' + ' ' + result );
